# Example DATABASE_URL for Prisma migration and runtime state storage
# Use SQLite locally instead of Postgres/Supabase/Neon
DATABASE_URL="file:./dev.db"

# Required for task agent loop (POST /api/tasks)
OPENAI_API_KEY="sk-..."
OPENAI_MODEL="gpt-4o-mini"
